// ---------------------------------------------------------------------------
// D'Vantage — Content Script Stub
//
// Runs on matched job board pages at document_idle (see manifest.ts).
// Full job detection, form field mapping, and autofill implemented in
// Week 2–3 via per-site adapter files in src/content/sites/*.ts.
// ---------------------------------------------------------------------------

console.log('[DVantage Content] Loaded on', window.location.hostname);
