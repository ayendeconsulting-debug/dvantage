/**
 * @vantage/worker-inbox — stub
 * Full worker implementation wired in the milestone that owns its queues.
 */
async function bootstrap(): Promise<void> {
   
  console.log('[worker-inbox] stub — full bootstrap wired in its milestone');
}

void bootstrap();
