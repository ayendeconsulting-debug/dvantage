/**
 * Drizzle schema barrel.
 * Tables are added here as each milestone builds out domain modules.
 * The drizzle() client receives the full schema for type-safe relations.
 */

// Milestone 0 — no tables yet; connection validated via raw query
// Milestone 1 — users, sessions (added in M1)
// Milestone 2 — resumes, resume_versions (added in M2)
// Milestone 3 — job_descriptions, ats_scores (added in M3)
// Milestone 4 — subscriptions, usage_events (added in M4)
// Milestone 5 — applications (added in M5)

export {};
