/**
 * @vantage/ui-kit
 *
 * Atlas design system — tokens, base components, and utilities.
 *
 * M0-B: tokens/ populated with full Atlas token set
 * M1+:  components/ populated as Auth UI is built
 *
 * Rule: any UI file in apps/web that references a color, font,
 * spacing value, or motion duration MUST import it from here.
 * Never raw hex. Never magic numbers.
 */

// Design tokens — populated in M0-B
export * from './tokens/index';
